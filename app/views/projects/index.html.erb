<div class="mb-3 d-flex justify-content-end">
  <button
    class="btn btn-primary"
  >
    Add Group
  </button>
</div>
<div class="d-flex overflow-x-scroll">
  <div id="new-group"></div>
  <% projects.each do |group_name, projects| %>
    <%= render 'group', group_name:, projects: %>
  <% end %>
</div>
